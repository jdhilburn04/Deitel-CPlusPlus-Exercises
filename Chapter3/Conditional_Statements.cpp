#include <iostream>

int main() {

    int studentGrade1{};
    std::cout << "Enter a grade: ";
    std::cin >> studentGrade;

    // if statement
    if (studentGrade < 70) {
        std::cout << "You failed";
    }

    int

    // if else (Unnessary Example
    if (studentGrade < 70) {
        std::cout << "You failed";
    } else {
        std::cout << "You passed";
    }

    // switch statement


    return 0;
}